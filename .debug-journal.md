# Debug Journal — CI behavior failures
Started: 2026-09-26 Asia/Seoul
Goal: diagnose and minimally fix PR-caused CI failures in this CI phase.

## Environment snapshot
- Runtime: Bash 3.2 CI and Node 22 CI; local Node 22.23.2.
- Git HEAD: 0c2ddf871224642a5d6a9c4b5e0a82c39b5f24d4; clean before this journal.
- References read: debugging setup, flaky triage, Node runtime, fix, QA.

## Hypotheses and evidence
1. Confirmed: fm-branch-dispatch CLI calls two functions absent from its shared module; focused host test reproduced a TypeError.
2. Confirmed: inactive reconcile invokes fm_dod_accept_ship_done without sourcing its defining library; CI observed command not found.
3. Confirmed: delayed merge tests expected a generic `away` label even though the merge-authority model records the precise `yolo` or `away-grant` source.
4. Pending: watcher failures may be races or test timing; rerun selected failures in isolation before changing code.

## Artifacts to revert
- `.debug-journal.md` — remove after verification.
- Source fixes — retain only if focused behavior checks pass.

## Planned source changes
- `.pi/extensions/lib/fm-branch-dispatch.ts` and `.pi/extensions/fm-branch-supervision.ts` — provide and reuse shared wake-prompt rendering used by the host CLI; retain only after host and Pi behavior checks pass.
- `bin/fm-inactive-reconcile.sh` — source DOD dependencies for its existing acceptance call; retain only after secondmate ledger tests pass.
- `tests/fm-pr-check-security.test.sh` — align the archived merge authority assertion with the source authority recorded for yolo and explicit-grant cases; retain only after the focused check passes.
- `tests/fm-pr-check-security.test.sh` — correct the words-only fixture to include the explicit task grant required by the selected policy; retain only after the focused check passes.
- `tests/fm-pr-check-security.test.sh` — align the concurrent teardown assertion with the persisted `yolo` source; retain only after the focused check passes.
